version: '3.3'

services:
  db:
    image: mysql:8.0 
    volumes:
      - db_data:/var/lib/mysql
    restart: always
   
    environment:
      MYSQL_ROOT_PASSWORD: QV=5bVX2 
      MYSQL_DATABASE: wordpress 
      MYSQL_USER: wordpressuser 
      MYSQL_PASSWORD: 7k0xAS*x 

  wordpress:
    depends_on:
      - db
    image: test:latest 
    ports:
      - 192.168.0.46:80:80
    restart: always
    volumes:
      - ./wp-content:/var/www/server-infra-s4.ru/wordpress/wp-content
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_USER: wordpressuser 
      WORDPRESS_DB_PASSWORD: 7k0xAS*x 
      WORDPRESS_DB_NAME: wordpress 

volumes:
    db_data: {}

